import 'package:flutter/material.dart';

abstract class IColors {
  static const backgroundLight = Color.fromARGB(255, 255, 255, 255); // #5468FF

  // Dark mode
  static const backgroundDark = Color(0xFF101213); // #5468FF
  static const transparent = Color(0x00000000); // #00000000
  static const attentionColor = Color(0xFF39D2C0); // #3D56F0
  static const errorColor = Color(0xFFEF4444);
  static const successColor = Color(0xFF10B981);
  static const iconsColorArteFacts = Color(0xFF101213);

  static const navbarTextColor = Colors.white;
}
